# Rebuilt Gentoo Profile

This directory is reserved for Rebuilt Gentoo profile customizations layered on top of upstream Gentoo.

The project aims to stay compatible with Gentoo while providing curated defaults, branding, desktop configuration, and ISO-specific settings.

Upstream Gentoo files are synchronized into `gentoo/` by GitHub Actions.
